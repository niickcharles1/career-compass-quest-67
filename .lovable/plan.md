## Goal
Add two new top-nav entries — **Cover Letter** and **Interview Coach** — that route to polished "coming soon" preview pages. No functional logic; just visual previews of what each feature will look like.

## Changes

### 1. Nav — `src/components/cc/Nav.tsx`
Add two new links to both desktop and mobile menus, between Translator and Actions:
- `Cover Letter` → `/cover-letter`
- `Interview Coach` → `/interview-coach`

Each gets a small "Soon" badge (teal pill) to set expectations.

### 2. New route — `src/routes/cover-letter.tsx`
Coming-soon preview for an AI Cover Letter Generator tied to a job description:
- Hero: title, subtitle, "Coming Soon" badge, brief explainer.
- Mock UI (disabled/non-interactive):
  - Left column: "Paste job description" textarea (read-only, prefilled placeholder text) + "Your role/experience" field + a disabled "Generate Cover Letter" button.
  - Right column: a styled preview card showing a sample generated cover letter (placeholder paragraphs) with greyed-out copy/download buttons.
- "What it will do" — 3 feature bullets (tailored to JD, matches your strengths from assessment, editable output).
- Notify-style CTA at bottom linking back to `/assessment`.
- `head()` with route-specific title + meta description + og tags.

### 3. New route — `src/routes/interview-coach.tsx`
Coming-soon preview for an AI Interview Coach:
- Hero: title, subtitle, "Coming Soon" badge.
- Mock UI:
  - "Select your target role" pill selector (static, e.g. Consulting / Product / Marketing / Finance).
  - Sample question card with a fake mic button, transcript area, and a disabled "Get Feedback" button.
  - Sample feedback panel showing mock scoring (Structure, Clarity, Impact) and one example tip.
- "What it will do" — 3 feature bullets (role-specific questions, spoken or written answers, instant feedback).
- `head()` with route-specific title + meta.

### 4. Route tree
`src/routeTree.gen.ts` is auto-generated by the Vite plugin; new route files will be picked up automatically.

## Out of scope
- No backend, no AI calls, no forms that submit, no DB.
- No changes to existing routes (`/`, `/assessment`, `/translator`, `/dashboard`, `/actions`, `/results`).
- No auth.

## Design
Reuses existing tokens: Deep Blue primary, Teal accent, Warm Orange CTA, white cards with `shadow-card`, matching the current site's look.
